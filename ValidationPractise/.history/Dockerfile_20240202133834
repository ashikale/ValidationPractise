FROM openjdk:11

ARG WORKING_DIR=/opt/validation-practise
ARG ARTIFACT_NAME=ValidationPractise-0.0.1-SNAPSHOT.jar

RUN MKDIR ${WORKING_DIR}
WORKDIR ${WORKING_DIR}

COPY target/${ARTIFACT_NAME} ${WORKING_DIR}